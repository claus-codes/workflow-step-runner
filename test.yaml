- step_1:
    assign:
    - numEntries: 1
    - timePeriod: month
- step_2:
    call: http.get
    args:
      url: https://www.reddit.com/r/videos/top.json
      query:
      - limit: ${numEntries}
      - t: ${timePeriod}
    result: response
- step_3:
    call: console.log
    args: ${response}
